# Branch for UwUipy v0.1.9
**Module call error for uwuipy**

The uwuipy version I developed main on is v0.1.7 which I installed through pip. This one only runs with ```from uwuipy import uwuipy```. 

The latest version on GitHub is v0.1.9, which specifies ```from uwuipy import Uwuify``` and lets you change the power in the constructor with an 8th argument.
